# Environment variables for VPS deployment
# Copy file này thành .env trên VPS và điền các giá trị

# === Database Configuration ===
POSTGRES_PASSWORD=your_secure_postgres_password_here

# === NextAuth Configuration ===
# URL công khai của ứng dụng (domain hoặc IP của VPS)
NEXTAUTH_URL=http://your-vps-ip:3000
# hoặc với domain: https://ztnet.yourdomain.com

# Secret key cho NextAuth (generate bằng: openssl rand -base64 32)
NEXTAUTH_SECRET=your_nextauth_secret_here

# === Docker Image Configuration ===
# Chọn image version để deploy:
# DOCKER_IMAGE=sinamics/ztnet:latest           # Latest build
# DOCKER_IMAGE=sinamics/ztnet:main-abc1234     # Specific commit
# DOCKER_IMAGE=sinamics/ztnet:v1.0.0           # Specific version
# DOCKER_IMAGE=ghcr.io/username/ztnet:latest   # GHCR registry
DOCKER_IMAGE=sinamics/ztnet:latest

# === Application Port ===
# Port để expose ra ngoài (default: 3000)
APP_PORT=3000

# === Optional: Email Configuration ===
# Nếu muốn sử dụng email notifications
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
